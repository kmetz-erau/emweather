'use client';
import type { WeatherData } from '@/types';

const colors = { extreme: 'text-red-300 bg-red-950/40 border-red-800', high: 'text-orange-300 bg-orange-950/30 border-orange-800', moderate: 'text-yellow-300 bg-yellow-950/20 border-yellow-800', low: 'text-slate-400 bg-[#0d1117] border-[#30363d]' };
export default function RiskIntelligencePanel({ weather }: { weather: WeatherData[] }) {
  return <div className="grid lg:grid-cols-2 gap-4">{weather.map(w => <div key={w.location.name} className="bg-[#161b22] border border-[#21262d] rounded-lg overflow-hidden"><div className="px-4 py-3 border-b border-[#21262d] flex justify-between"><div><div className="text-[10px] text-slate-500 font-mono uppercase tracking-widest">Operational Risk Intelligence</div><div className="text-xs text-slate-300 mt-1">{w.location.name}</div></div><span className={`h-fit px-2 py-1 rounded border text-[10px] font-mono uppercase ${colors[w.riskProfile.overall]}`}>{w.riskProfile.overall}</span></div><div className="p-3 space-y-2">{w.riskProfile.risks.map(r => <details key={r.type} className={`rounded border ${colors[r.severity]}`} open={r.severity === 'high' || r.severity === 'extreme'}><summary className="cursor-pointer list-none px-3 py-2 flex items-center justify-between"><span className="text-xs font-mono">{r.type}</span><span className="text-[10px] font-mono">{r.score}/100 · {r.timing}</span></summary><div className="px-3 pb-3 border-t border-current/10 pt-2"><div className="text-[10px] uppercase font-mono opacity-60 mb-1">Evidence</div>{r.evidence.map(e => <p key={e} className="text-xs opacity-90">• {e}</p>)}<div className="text-[10px] uppercase font-mono opacity-60 mt-2 mb-1">Actions</div>{r.actions.map(a => <p key={a} className="text-xs opacity-90">• {a}</p>)}</div></details>)}</div></div>)}</div>;
}
